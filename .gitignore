__pycache__/
*.pyc
.DS_Store
Thumbs.db
*.log
.env

# Готовый сайт — пересобирается из data/ и _category_template.html
docs/

# Резервные копии редактируемых файлов
_backup/

# ВАЖНО: data/ НЕ игнорируется — это архив проекта, он должен лежать в git.
# Раньше накопители жили в кэше GitHub Actions и удалялись после 7 дней простоя.
